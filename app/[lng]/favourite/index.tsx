import React, { Fragment } from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Favourite | Bond Reality",
    description: "Check out your favourite items from the Bond Shop.",
};

const FavouritePage = () => {
    return (
        <Fragment>
            <div>index</div>
        </Fragment>
    );
};

export default FavouritePage;
